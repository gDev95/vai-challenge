import styled from "styled-components";
import { LinearProgress } from "@material-ui/core";

const YearProgress = styled(LinearProgress)`
	margin-left: 5px;
	margin-right: 5px;
`;

export default YearProgress;
